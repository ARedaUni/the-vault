// Loaded by the `browser` project (see vitest.config.ts) so every spec renders
// with the same styles the running app uses. Without this the test iframe
// renders unstyled, and a component can pass here while looking broken in
// `e2e/`.
import '../index.css'
